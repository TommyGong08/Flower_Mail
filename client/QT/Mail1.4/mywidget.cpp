#include "mywidget.h"

MyWidget::MyWidget(QObject *parent) : QObject(parent)
{
//    this->lo_cnt = new LoginWindow;
//    this->re_cnt = new RegisterWindow;

//    connect(re_cnt,&RegisterWindow::registerClose,
//            lo_cnt,&LoginWindow::loginOpen);

}
